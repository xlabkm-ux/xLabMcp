# Mutation Policy

- No broad scene/prefab/graph churn without a plan.
- `ProjectSettings/` and `Packages/` changes need explicit reason and summary.
- Separate safe-by-default and confirm-required settings.
- End every meaningful mutation with verification.
